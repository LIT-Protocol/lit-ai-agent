{
  "tasks": {
    "bundle": "deno run --allow-read --allow-write --allow-env --allow-run src/bundle.ts",
    "deploy": "deno task bundle && deno run --allow-read --allow-write --allow-env --allow-net --allow-run src/deploy.ts"
  },
  "imports": {
    "esbuild": "npm:esbuild"
  }
}
